# Conversation Hub

Unified inbox frontend and API aggregating messages from all channels (web, WhatsApp, Instagram, FB Messenger) into a single interface. Built with TypeScript and React.

Run locally: `npm install && npm run dev` (requires Node 18+)