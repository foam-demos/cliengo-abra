# WhatsApp Connector

Handles WhatsApp Business API integration using Baileys. Manages socket connections, message routing, and credential storage in MongoDB.

Run locally: `npm install && npm start` (requires Node 18+, MongoDB 4.4+)