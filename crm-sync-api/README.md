# CRM Sync API

Manages lead capture, contact data synchronization, and third-party CRM integrations (HubSpot, Salesforce). Built with Play Framework and MongoDB.

Run locally: `sbt run` (requires MongoDB 4.4+, Scala 2.13, Redis 6+)